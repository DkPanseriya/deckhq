{
  "_comment": "NOT PRODUCT CODE. WP-19 spike artefact. Pass it with `claude --settings scripts/spike-permission/settings.sample.json` so it layers on top of your real scopes without writing to any of them. Never merge this into ~/.claude/settings.json by hand.",
  "hooks": {
    "PermissionRequest": [
      {
        "hooks": [
          {
            "type": "http",
            "url": "http://127.0.0.1:4319/api/permission",
            "timeout": 600,
            "statusMessage": "Waiting for DeckHQ…",
            "_deckhq": true
          }
        ]
      }
    ]
  }
}
