reference/
node_modules/
docs/
*.log
vscode/.vscodeignore

# The documentation site's build output. `site/` itself is formatted; what
# `node site/build.mjs` renders into `site/dist/` is generated, gitignored, and
# not a file anybody edits.
site/dist/

# The private planning repository — WP-95b. Its own repository, its own
# formatting; absent on every machine but a maintainer's.
internal/
