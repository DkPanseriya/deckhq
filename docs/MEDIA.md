# Media — what every image is, and what it may be called

**Status:** policy, opened 15 September 2026 (WP-94a) · **Owner:** orchestrator ·
**Enforced by:** `site/build.mjs` and `test/unit/site.test.mjs`

Every picture this project publishes is one of three things, and the picture has to say which. A
mockup shown as a screenshot is the same defect as an unverified adapter claiming to be verified,
and [`ADAPTERS.md` §6](ADAPTERS.md) is the rule it breaks: *a claim in anyone's documentation is a
hypothesis until measured on a machine.* A drawing of a floor nobody has rendered is a claim.

---

## 1. The three classes

| Class | What it is | Where it comes from |
|---|---|---|
| `capture` | DeckHQ, running, photographed — a real floor or a fixture floor, taken through `scripts/capture-floor.mjs` or `scripts/capture-hero.mjs` | `docs/media/*.png`, `docs/media/hero.gif` |
| `golden` | A real render of a fixture population, taken by the visual-regression gate on the code that is on `main` | `test/goldens/win32/*.png` |
| `illustration` | A designer's mockup of a specification. **Not shipped code.** It shows what a package intends, not what a build does | `docs/media/interior/`, `docs/media/look/`, `docs/media/motion/`, `docs/media/design/` |

A `golden` is a capture in every technical sense — it is the renderer's own output — and it is
kept a separate class because it is the only class whose currency is **proved on every run**. A
golden that drifted by one pixel fails CI; a capture in `docs/media/` drifts silently and is only
found by somebody looking at it.

## 2. The rules

1. **Every image the site or the README shows carries its class in its caption.** On the site that
   is a chip at the end of the `<figcaption>` — `Capture`, `Golden render`, `Design illustration`.
   In the README it is the words in the caption line.
2. **An illustration is labelled `design illustration` and is never described as the product.** Its
   caption says what package it belongs to and whether that package has shipped. "This is what
   WP-88 will look like" is allowed; "this is the control centre" is not.
3. **The build fails rather than the reader being misled.** `site/build.mjs` knows the class of
   every image it copies. If a page shows an image sourced from `docs/media/interior`, `look`,
   `motion` or `design` and its caption does not contain the words *design illustration*, the build
   throws and the Pages workflow goes red. `test/unit/site.test.mjs` asserts the same thing against
   the emitted pages, so the gate cannot be removed from one side only.
4. **A stale capture is recaptured, never relabelled.** A capture taken before a visual package
   landed is still a capture; it is just a photograph of an older floor. §4 lists them, and
   WP-94b recaptures them. None of them may be presented as the floor today without that note in
   the caption.
5. **Nothing is invented.** No image in this repository is generated by an image model, and none
   ever will be: every one of them is either a screenshot of this product or a vector drawn by a
   script in this repository, and the script is committed beside it.

## 3. Size

Images are copied into the site at build time. A PNG wider than **1600 px** is downscaled to
1600 px on the way, by `site/build.mjs` using `scripts/lib/png.mjs` — the same dependency-free
decoder the goldens harness uses. Nothing else is touched: a GIF is copied byte for byte, and a
PNG at or under 1600 px is copied byte for byte, so what the site serves for those is the file that
is in the repository.

1600 px is the capture stage (`scripts/goldens.mjs` `DEFAULT_WIDTH`), so a golden is never
resampled and a golden on the site is the golden in the tree.

**Every published image also carries its own pixel size in the markup** — WP-94c. A picture without
`width` and `height` is a picture the browser reserves no room for, so the words under it move when
it arrives. `addImageDimensions()` in `site/build.mjs` reads the size out of the copy the site
serves — a PNG through `scripts/lib/png.mjs`, a GIF out of its thirteen-byte header — and writes the
pair onto every `media/` image on every hand-written page and every rendered log entry. A tag that
already declares a size is left alone, because a hand-written pair is a deliberate one, and
`test/unit/site.test.mjs` checks every declared pair against the file on disk rather than merely
checking one is present.

## 4. The register

Current = a fair picture of what `main` renders today. Stale = still a real photograph, of an older
build. The two visual packages that made most of the older captures stale are **WP-79**
(`DEVIATIONS.md` §162 — the doodle became a robot) and **WP-85a/b/c** (§160, §163, §164 — one key
light, the furniture set, props and the four-bay lounge).

### 4.1 Goldens — `test/goldens/win32/`, class `golden`, all current

Taken by `npm run goldens` on the code on `main`, on a 1600 × 1000 stage, and re-checked on every
CI run.

| File | Fixture | Current? | On the site |
|---|---|---|---|
| `three.png` | `three` — three active repos, nine sessions | current | Home, hero |
| `demo.png` | `demo` — the full demo population | current | Features, Look |
| `pinned.png` | `three` with one repo pinned | current | Features (pinning) |
| `single.png` | `single` — one project | current | Characters (what ships now) |
| `empty.png` | `empty` — nobody on the floor | current | — |
| `reference.png` | `reference` — the reference machine's shape | current | — |
| `wide.png` | `three` on a 1920 × 1080 stage | current | — |
| `demo@night-shift.png` | `demo`, night shift theme | current | Features (themes), Look |
| `demo@blueprint.png` | `demo`, blueprint theme | current | Look |

### 4.2 Captures — `docs/media/`, class `capture`

| File | What it shows | Package | Current? | On the site |
|---|---|---|---|---|
| `hero.gif` | A turn ending: desk → corridor → the queue | WP-03, §88 | **stale** — pre-WP-79 figures | Home |
| `deck-view.png` | The deck: the queue as a table | WP-10, §103 | **stale** — pre-WP-79 | Features |
| `panel-review-card.png` | The review card: what it said, what changed, three actions | WP-08, §85 | **stale** — pre-WP-79 | Features |
| `permission-card.png` | A permission prompt answered from the panel | WP-19, §97 | **stale** — pre-WP-79 | Features |
| `app-window.png` | The floor in a Chrome application window | WP-62, §144; re-shot at WP-82, §161 | **stale** — pre-WP-79 | Features |
| `office-cleared.png` | The cleared office, the state the sounds mark | WP-15, §110 | **stale** — pre-WP-79 | Features |
| `wrapped-weekly.png` | Wrapped: turns, tokens, spend, the longest wait | WP-27, §119 | **stale** — pre-WP-79 | Features |
| `identity.png` | Faces, names and hats | WP-20, §105 | **stale** — pre-WP-79 | the log |
| `juniors.png` | Sub-agents beside their parent | WP-41, §120 | **stale** — pre-WP-79 | the log |
| `floor.png` | The floor | WP-03 | **stale** — pre-WP-79 | — (replaced by `three.png`) |
| `panel.png` | The panel inside VS Code | WP-31, §104 | stale | — |
| `panel-diff.png` | The in-panel diff | WP-47, §90 | stale | — |
| `queue-strip.png` | The queue strip under the header | WP-10 | stale | — |
| `header-palette.png` | Header, `⌘K` palette, settings sheet | WP-07, §94 | stale | — |
| `settings-sheet.png` | The settings sheet | WP-07, §94 | stale | — |
| `pack-picker.png` | The theme picker with a Supporter pack installed | WP-45, §129 | stale | — |
| `theme-night-shift.png` | The night shift theme | WP-30, §125 | stale | — (replaced by the golden) |
| `theme-blueprint.png` | The blueprint theme | WP-30, §125 | stale | — (replaced by the golden) |
| `thought-bubble.png` | Thought bubbles | WP-52, §89 | stale | — |
| `postcard.png` | The day's card | WP-18, §118 | stale | — |
| `snapshot-sample.png` | An office snapshot, redacted | WP-14, §109 | stale | — |
| `coach-marks.png` | The first-run coach marks | WP-13, §108 | stale | — |
| `mini-floor.png` | The floating mini-floor | WP-39, §113 | stale | — |
| `site-index.png` | This documentation site | WP-29, §112 | **stale** — predates this package | — |
| `demo-before-wp50.png`, `demo-after-wp50.png`, `floor-before-wp50.png`, `floor-after-wp50.png` | Before/after, the dynamic floor | WP-50, §96 | historical — **never recapture** | the log |
| `demo-before-wp55.png`, `demo-after-wp55.png`, `floor-before-wp55.png`, `floor-after-wp55.png` | Before/after, the content-sized building | WP-55, §106 | historical — **never recapture** | the log |

A before/after pair is a record of a change and is stale **on purpose**. Recapturing one would
destroy the evidence it exists to carry. They are never republished as the product.

### 4.3 Illustrations — class `illustration`, none of them shipped code

| File | What it shows | Package | Ships? | On the site |
|---|---|---|---|---|
| `look/presets.png` | Six curated interior presets | WP-88a | **planned** | Look |
| `look/control-centre.png` | The graphics control centre | WP-88b | **planned** | Look |
| `look/agent-sizes.png` | Agent size, small to large, furniture scaling with it | WP-88c | **planned** | Look |
| `interior/board.png` | The material board: grounds, woods, fabrics, metals | WP-85 | shipped as materials; the board itself is a drawing | Look |
| `interior/mockup-default.png` | The designed interior, default theme | WP-85 | shipped (§163, §164) | Look |
| `interior/mockup-night-shift.png` | The designed interior, night shift | WP-85 | shipped | — |
| `interior/mockup-blueprint.png` | The designed interior, blueprint | WP-85 | shipped | — |
| `interior/crop-project-room@2x.png` | A project room at 2× | WP-85 | shipped | — |
| `interior/crop-reception@2x.png` | The reception at 2× | WP-85 | shipped | — |
| `design/character/B.png` | Character candidate B, the sheet the owner picked | WP-79 | **shipped** as the figure; the sheet is the proposal | Characters |
| `design/character/in-situ.png` | All four candidates on one lounge crop | WP-79 | proposal only | Characters |
| `design/icon/sheet.png` | Five candidate marks at every size | WP-82 | candidate 5 shipped | — |
| `design/icon/5.svg`, `5-dark.png`, `5-light.png` | The chosen mark, as drawn for the sheet | WP-82 | superseded by `public/brand/deckhq-mark.svg` | — |
| `motion/life-sheet.png` | The twelve ways the figure moves | WP-87 | **planned** | Characters |
| `motion/lounge-activities.png` | What a benched agent does in the lounge | WP-87 | **planned** | Characters |
| `motion/crew.gif` | A crew of sub-agents arriving around a parent | WP-89 | **planned** | Characters |
| `motion/crew.png` | The same crew, as a sheet | WP-89 | **planned** | — |

The sheets under `design/` and `motion/` are rendered by scripts committed beside them
(`sheet.js`, `life-sheet.js`, `lounge.js`, `crew.js`, `render.mjs`), from the same `Path2D` drawing
code the floor uses. That is what makes them faithful mockups rather than guesses — and it is not
what makes them screenshots, because nothing in the product calls them.

### 4.4 The site itself — `docs/media/site/`, class `capture`, all current

Photographs of the documentation site, taken by `node site/capture.mjs` — WP-94c. It builds the
site into a temp directory, serves it from a socket the OS chooses, drives one Chrome over the
DevTools Protocol and takes the whole lot down again; the only origin in play is `127.0.0.1` on a
port that existed for a few seconds. They are **captures**, not illustrations: every pixel is the
real build rendered by a real browser.

They are a record of how the site looked when WP-94c landed and are **not published on the site**
— a picture of a page, on that page, is a loop nobody needs. Retake them with the same command
whenever the site's design changes.

| File | What it shows |
|---|---|
| `site/home-375-light.png`, `site/home-375-dark.png` | The home page on a 375 × 812 phone, both schemes. The hero is the `three` golden at 2.6×, cropped to your office |
| `site/home-768-light.png`, `site/home-768-dark.png` | The home page on a 768 × 1024 tablet, both schemes |
| `site/home-1440-light.png`, `site/home-1440-dark.png` | The home page on a 1440 × 900 desktop, both schemes |

The same run measures what a screenshot cannot show and fails on any of it: horizontal overflow on
thirteen pages at 375 and 1440, a reveal that did not resolve, a section still hidden under
`prefers-reduced-motion`, a page with other than one `<h1>`, a bar that is not sticky, a scheme
toggle that stayed hidden, or a picture that did not load.

`site-index.png` in §4.2 is the older, unpublished picture of the site and is superseded by these.

## 5. WP-94b — the recapture list

After **WP-89** lands, every file marked **stale** in §4.2 that is still published is retaken on
the current build, and the hero GIF is re-recorded. Until then the site prefers a golden wherever
one exists, which is why the Home hero is `three.png` rather than `floor.png`.

Not recaptured, ever: the eight before/after files in §4.2, and `site-index.png`, which is a
picture of the site this package rewrote and is replaced rather than retaken.
